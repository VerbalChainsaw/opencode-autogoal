# Goal runtime state (never committed — per-session ephemeral data)
.opencode/.goal-state.json

# OS files
Thumbs.db
Desktop.ini

# Node (plugin dependencies — auto-installed by OpenCode)
node_modules/
package-lock.json
