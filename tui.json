{
  "$schema": "https://opencode.ai/config.json",
  "//": "Optional terminal-only dashboard. This file is a DEV dogfood: when",
  "//": "developing inside the repo, `./src/tui.tsx` loads the local source",
  "//": "(Bun compiles the JSX on the fly). When CONSUMING the npm package,",
  "//": "use the published subpath `opencode-autogoal/tui` — see README.md.",
  "//": "The TUI is disabled by default because it can only be verified in a",
  "//": "live OpenCode TUI; ship it as opt-in.",
  "plugin": [
    "./src/tui.tsx"
  ]
}
